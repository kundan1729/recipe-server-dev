# Example environment variables for Recipe AI backend
# Copy this file to `.env` and fill in the real values.

# Port the app listens on (Render will set this automatically in production)
PORT=5000

# MongoDB connection string - replace placeholders with your DB credentials
# Example format for MongoDB Atlas:
# mongodb+srv://<DB_USER>:<DB_PASS>@cluster0.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<DB_USER>:<DB_PASS>@cluster0.mongodb.net/<DB_NAME>

# JWT secret used to sign tokens (keep this secret!)
JWT_SECRET=your_jwt_secret_here

# Node environment: development or production
NODE_ENV=development

# Frontend origin allowed for CORS (set to your Vercel URL)
FRONTEND_URL=https://recipe-client-dev-z6ev.vercel.app

# Optional: override default Render port (not usually needed)
# PORT=10000
